# -*- mode: org; -*-

* Personal Emacs Configuration

#+CAPTION: 从命令行启动 Emacs 所需的最少时间
#+BEGIN_SRC sh :results output
  (time emacs -l init.elc --batch --eval '(message "Hello, world!")') 2>&1
#+END_SRC

#+RESULTS:
: Hello, world!
: 
: real	0m1.756s
: user	0m1.505s
: sys	0m0.217s

** To Do items

*** TODO 改进 Emacs 版本支持，需要支持 24.5+，可以考虑用 CI 验证

现在需要至少 Emacs 25
